apiVersion: v1
kind: Service
metadata:
  name: {{ include "bw-echo.fullname" . }}
  labels:
    {{- include "bw-echo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bw-echo.selectorLabels" . | nindent 4 }}
